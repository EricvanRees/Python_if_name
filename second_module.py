# importing a module runs the code inside it
# it prints "First module's name: first_module"
import first_module

# prints "Second module's name: __main__" when run here:
print(f"Second module's name: {__name__}")
